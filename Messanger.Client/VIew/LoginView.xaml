<Window x:Class="Messanger.Client.VIew.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Messanger.Client.VIew"
        xmlns:viewModels="clr-namespace:Messanger.Client.ViewModel"
        mc:Ignorable="d"
        Title="LoginView" Height="350" Width="600">
    <Window.DataContext>
        <viewModels:LoginViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Label Content="Введите ваш никнейм" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
        <TextBlock Text="{Binding Username}" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="1"/>
    </Grid>
</Window>
